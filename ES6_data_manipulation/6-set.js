// Archivo: 6-set.js

// Definir la función setFromArray
function setFromArray(array) {
  // Crear un nuevo Set a partir del array proporcionado
  return new Set(array);
}

// Exportar la función setFromArray para que pueda ser utilizada en otros archivos
export default setFromArray;
